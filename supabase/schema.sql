-- Supabase Schema Tracking
-- Use this file to track all database schema changes.
-- This file serves as the source of truth for your database structure.

-- Example:
-- create table public.users (
--   id uuid references auth.users not null,
--   email text,
--   created_at timestamptz default now()
-- );
